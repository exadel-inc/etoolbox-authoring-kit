<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <artifactId>aem-authoring-toolkit-samples</artifactId>
        <groupId>com.exadel.aem</groupId>
        <version>1.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>toolkit-samples-package</artifactId>
    <name>AEM Authoring Toolkit - Samples Package</name>

<!--    <packaging>content-package</packaging>-->

    <properties>
        <host>localhost</host>
        <port>4502</port>
        <serviceUrl>http://${host}:${port}/crx/packmgr/service.jsp</serviceUrl>
        <vault.user>admin</vault.user>
        <vault.password>admin</vault.password>
        <project.jcrAppFolder>authoring-toolkit/samples</project.jcrAppFolder>
    </properties>

    <build>
        <finalName>aem-dialog-plugin-sandbox-package-${project.version}</finalName>
        <plugins>
            <plugin>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.1.2</version>
                <executions>
                    <execution>
                        <id>default-jar</id>
                        <phase>none</phase>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-install-plugin</artifactId>
                <version>2.5.2</version>
                <executions>
                    <execution>
                        <id>default-install</id>
                        <phase>none</phase>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>install-samples</id>
            <build>
                <resources>
                    <resource>
                        <directory>src/main/content/jcr_root</directory>
                        <excludes>
                            <exclude>**/*.vlt/**</exclude>
                            <exclude>**/*.vlt</exclude>
                            <exclude>**/*.DS_Store</exclude>
                        </excludes>
                        <targetPath>.</targetPath>
                    </resource>
                    <resource>
                        <directory>src/main/content/META-INF</directory>
                        <excludes>
                            <exclude>**/*.vlt/**</exclude>
                            <exclude>**/*.vlt</exclude>
                            <exclude>**/*.DS_Store</exclude>
                        </excludes>
                        <targetPath>../vault-work/META-INF</targetPath>
                    </resource>
                </resources>
                <plugins>
                    <plugin>
                        <groupId>com.day.jcr.vault</groupId>
                        <artifactId>content-package-maven-plugin</artifactId>
                        <version>0.5.1</version>
                        <extensions>true</extensions>
                        <configuration>
                            <targetURL>${serviceUrl}</targetURL>
                            <packaging>zip</packaging>
                            <userId>${vault.user}</userId>
                            <password>${vault.password}</password>
                            <filters>
                                <filter>
                                    <root>/apps/${project.jcrAppFolder}</root>
                                </filter>
                            </filters>
                            <embeddeds>
                                <embedded>
                                    <groupId>${project.groupId}</groupId>
                                    <artifactId>toolkit-samples-bundle</artifactId>
                                    <target>/apps/${project.jcrAppFolder}/install</target>
                                </embedded>
                            </embeddeds>
                        </configuration>
                        <executions>
                            <execution>
                                <id>Compile and install local application package</id>
                                <goals>
                                    <goal>package</goal>
                                    <goal>install</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>com.exadel.aem</groupId>
                        <artifactId>aem-authoring-toolkit-plugin</artifactId>
                        <version>${project.parent.version}</version>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>aem-authoring</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <!-- Place here the path to the node under which your component nodes are stored -->
                            <componentsPathBase>jcr_root/apps/${project.jcrAppFolder}/components</componentsPathBase>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <dependencies>
        <!-- Foreign -->
        <dependency>
            <groupId>com.adobe.aem</groupId>
            <artifactId>uber-jar</artifactId>
            <classifier>apis</classifier>
        </dependency>
        <!-- Internal -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>toolkit-samples-bundle</artifactId>
            <version>${project.parent.version}</version>
        </dependency>
    </dependencies>
</project>