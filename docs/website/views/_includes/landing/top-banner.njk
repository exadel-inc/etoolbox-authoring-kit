{% from 'landing/banner.njk' import banner %}
<eak-banner class="banner banner-top">
  {{ banner({
    title: landing.marquee.title,
    description: landing.marquee.description,
    buttons: landing.marquee.buttons,
    bgIcon: 'banner-waves.svg',
    top: true
  }) }}
</eak-banner>
