<div class="article-container container d-flex mb-150">
    <div class="article-title">
        <h3 class="m-2">Exadel Authoring Kit Digest</h3>
        <p class="fw-300 m-2">
            Insights on AEM and Adobe Experience Manager use cases with technical tips and tricks from the creators of <span>MarTech solutions</span>
        </p>
    </div>
    <div class="article-list d-flex">
        {% for article in landing.articleSection %}
        <a class="article-link d-block" href="{{article.linkUrl}}">
        <div class="article-item text-white">
            <h5 class="fs-1 article-item-title mb-2">{{article.title}}</h5>
            <div class="d-flex article-item-img-container mb-1"><img class="article-item-img" src="{{article.imgUrl}}"></img></div>
            <ul class="article-item-taglist d-flex mb-1">
            {% for tag in article.tags %}
                <li class="article-item-taglist-tag">#{{tag}}</li>
            {% endfor %}
            </ul>
            <p class="article-content fw-300 fs-1 mb-1">{{ article.content }}</p>
            <span class="article-item-date">{{ article.date }}</span>
        </div>
        </a>
        {% endfor %}
    </div>
    <div class="z-index d-flex article-center">
        <a href="{{landing.urls.articleExadelUrl}}"class="btn btn-mint m-0">Learn more</a>
    </div>
</div>
