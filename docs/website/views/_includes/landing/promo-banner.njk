{% from 'landing/banner.njk' import banner %}
<eak-banner class="banner banner-promo" targets-number="0">
  {{ banner({
    title: landing.promo.title,
    description: landing.promo.description,
    buttons: landing.promo.buttons,
    bgIcon: 'promo-banner-waves.svg'
  }) }}
</eak-banner>
